% !TeX root = main.tex
\documentclass[a4paper]{article}

% Template settings formerly provided by a separate style file.
% This guide ships in draft mode (\submissionfalse) so the demo todo
% notes and the todo list stay visible.
% Set \submissiontrue for a clean submission PDF.
% Set \usebiblatexfalse for BibTeX/natbib citations.
\newif\ifsubmission
\submissionfalse
\newif\ifusebiblatex
\usebiblatextrue

\newif\ifhasbibfile
\IfFileExists{refs.bib}{%
	\hasbibfiletrue
}{%
	\hasbibfilefalse
}

\DeclareFontShape{OT1}{cmr}{m}{scit}{<->ssub*cmr/m/sc}{}
\usepackage{slantsc}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{aliascnt}
\usepackage{amssymb}
\usepackage{graphicx}
\graphicspath{{figs/}}
\usepackage{booktabs}
\usepackage{multirow}
% dvipsnames provides BrickRed, used by the jeffe.sty \Comment macro below
\usepackage[dvipsnames]{xcolor}
\usepackage{mathrsfs}
\usepackage[utf8]{inputenc}
\usepackage{adjustbox}
\usepackage{microtype}
\usepackage{enumerate}
\usepackage{verbatim}
\usepackage{tikz}
\usetikzlibrary{shapes,calc,math,backgrounds,matrix}

\ifusebiblatex
\usepackage[%
backend=biber,
bibstyle=numeric,
citestyle=numeric-comp,
maxcitenames=3,
maxbibnames=20,
sorting=ydnt
]{biblatex}
\ifhasbibfile
	\addbibresource{refs.bib}%
\fi
\else
\usepackage[sort&compress,numbers,square]{natbib}
\bibliographystyle{plainnat}
\fi

\usepackage{hyperref}
\hypersetup{
	colorlinks,
	allcolors=blue,
	unicode,
	bookmarksopen,
	bookmarksdepth=2,
	pdftitle={An Article Template},
	pdfauthor={Anonymous},
	pdfkeywords={article template, placeholder text, LaTeX}
}

\usepackage[capitalise,noabbrev]{cleveref}
\crefname{cond}{Condition}{Conditions}
\creflabelformat{cond}{(#2C#1#3)}
\crefname{eitem}{}{}
\creflabelformat{eitem}{(#2#1#3)}
\crefname{step}{\textbf{Step}}{\textbf{Steps}}
\creflabelformat{step}{{\bf #2#1#3}}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newaliascnt{corollary}{theorem}
\newtheorem{corollary}[corollary]{Corollary}
\aliascntresetthe{corollary}
\newaliascnt{lemma}{theorem}
\newtheorem{lemma}[lemma]{Lemma}
\aliascntresetthe{lemma}
\newtheorem{claim}{Claim}[theorem]
\newaliascnt{axiom}{theorem}
\newtheorem{axiom}[axiom]{Axiom}
\aliascntresetthe{axiom}
\newaliascnt{conjecture}{theorem}
\newtheorem{conjecture}[conjecture]{Conjecture}
\aliascntresetthe{conjecture}
\newaliascnt{fact}{theorem}
\newtheorem{fact}[fact]{Fact}
\aliascntresetthe{fact}
\newaliascnt{hypothesis}{theorem}
\newtheorem{hypothesis}[hypothesis]{Hypothesis}
\aliascntresetthe{hypothesis}
\newaliascnt{assumption}{theorem}
\newtheorem{assumption}[assumption]{Assumption}
\aliascntresetthe{assumption}
\newaliascnt{proposition}{theorem}
\newtheorem{proposition}[proposition]{Proposition}
\aliascntresetthe{proposition}
\newaliascnt{criterion}{theorem}
\newtheorem{criterion}[criterion]{Criterion}
\aliascntresetthe{criterion}
\theoremstyle{definition}
\newaliascnt{definition}{theorem}
\newtheorem{definition}[definition]{Definition}
\aliascntresetthe{definition}
\newaliascnt{example}{theorem}
\newtheorem{example}[example]{Example}
\aliascntresetthe{example}
\newaliascnt{remark}{theorem}
\newtheorem{remark}[remark]{Remark}
\aliascntresetthe{remark}
\newaliascnt{problem}{theorem}
\newtheorem{problem}[problem]{Problem}
\aliascntresetthe{problem}
\newaliascnt{principle}{theorem}
\newtheorem{principle}[principle]{Principle}
\aliascntresetthe{principle}

\crefname{theorem}{Theorem}{Theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{corollary}{Corollary}{Corollaries}
\Crefname{corollary}{Corollary}{Corollaries}
\crefname{lemma}{Lemma}{Lemmas}
\Crefname{lemma}{Lemma}{Lemmas}
\crefname{claim}{Claim}{Claims}
\Crefname{claim}{Claim}{Claims}
\crefname{axiom}{Axiom}{Axioms}
\Crefname{axiom}{Axiom}{Axioms}
\crefname{conjecture}{Conjecture}{Conjectures}
\Crefname{conjecture}{Conjecture}{Conjectures}
\crefname{fact}{Fact}{Facts}
\Crefname{fact}{Fact}{Facts}
\crefname{hypothesis}{Hypothesis}{Hypotheses}
\Crefname{hypothesis}{Hypothesis}{Hypotheses}
\crefname{assumption}{Assumption}{Assumptions}
\Crefname{assumption}{Assumption}{Assumptions}
\crefname{proposition}{Proposition}{Propositions}
\Crefname{proposition}{Proposition}{Propositions}
\crefname{criterion}{Criterion}{Criteria}
\Crefname{criterion}{Criterion}{Criteria}
\crefname{definition}{Definition}{Definitions}
\Crefname{definition}{Definition}{Definitions}
\crefname{example}{Example}{Examples}
\Crefname{example}{Example}{Examples}
\crefname{remark}{Remark}{Remarks}
\Crefname{remark}{Remark}{Remarks}
\crefname{problem}{Problem}{Problems}
\Crefname{problem}{Problem}{Problems}
\crefname{principle}{Principle}{Principles}
\Crefname{principle}{Principle}{Principles}

\usepackage[linesnumbered,ruled,vlined,commentsnumbered]{algorithm2e}

% ----------------------------------------------------------------------
%  Algorithm and code environments from jeffe.sty by Jeff Erickson
%  (jeffe@cs.uiuc.edu), "macros I use everywhere", last modified
%  22 May 2010 -- "This is free; caveat emptor!"  The style is taken
%  from Jeff Erickson's website (https://jeffe.cs.illinois.edu/) and
%  is the pseudocode style of his textbook "Algorithms".  Ported here
%  together with the companion helpers \Comment, \mathsc, and \textul
%  used by that style.
%
%  From the original: "Algorithms are set in normal text, with 2em
%  indenting, surrounded by a box.  Code is set in typewriter text,
%  with 4 space indenting, and no box."
%
%  WARNING (also from the original)!  The true width of the minipage
%  environment is determined by the contents of the longest line,
%  because the minipage environment contains a tabbing environment
%  and nothing else.  It's a HaX!
%
%  Local changes for this template:
%  (1) jeffe.sty's `algorithm' environment is renamed `algobox' here,
%      because algorithm2e (loaded above) already defines `algorithm'.
%  (2) \tt is replaced by \ttfamily in the `code' environment.
% ----------------------------------------------------------------------
\makeatletter
\def\begin@lgo{\begin{minipage}{1in}\begin{tabbing}
        \quad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\qquad\=\kill}
\def\end@lgo{\end{tabbing}\end{minipage}}

\newenvironment{algobox}
{\begin{tabular}{|l|}\hline\begin@lgo}
{\end@lgo\\\hline\end{tabular}}

\newenvironment{algo}
{\begin{center}\small\begin{algobox}}
{\end{algobox}\end{center}}

% Pseudocode comments set as <<...>> in BrickRed.  ("Meh." -- Jeff)
\def\Comment#1{{\color{BrickRed}\textsf{\textsl{$\langle\!\langle$#1\/$\rangle\!\rangle$}}}}

\def\beginc@de{\noindent\begin{center}\begin{minipage}{1in}\ttfamily
        \begin{tabbing}~~~~\=~~~~\=~~~~\=~~~~\=~~~~\=~~~~\=~~~~\=~~~~\=\kill}
\def\endc@de{\end{tabbing}\end{minipage}\end{center}}

\newenvironment{code}{\beginc@de}{\endc@de}
\makeatother

% Small caps in math mode for algorithm names such as $\mathsc{MergeSort}$
% (from the TEXT STYLES section of jeffe.sty).
\def\mathsc#1{\text{\textsc{#1}}}
% Underline for pseudocode header lines; works on <1 line of text only
% (from jeffe.sty: "Use sparingly!!").
\def\textul#1{\underline{\smash{#1}\vphantom{.}}}

\usepackage[pagewise]{lineno}
\usepackage[colorinlistoftodos]{todonotes}

\definecolor{done}{rgb}{0.55, 0.71, 0.0}
\definecolor{highpriority}{rgb}{0.82, 0.1, 0.26}
\definecolor{lowpriority}{rgb}{1.0, 0.75, 0.0}
\definecolor{mediumpriority}{rgb}{1.0, 0.49, 0.0}

\newcommand{\Mcomment}[3]{%
\ifsubmission%
	\else%
	% Self-contained inline reviewer comment: \unskip\space normalizes
	% the spacing before the parenthetical, and the person's name sits
	% inside it. The former \marginpar person flag is gone; a bare name
	% alone in the margin read as a stray word.
	\unskip\ {\color{#1}\bfseries\sffamily(#2: #3)}%
	\fi%
}
\newcommand{\TaskNewPerson}[3]{%
	\expandafter\newcommand\csname #2\endcsname[1]{\Mcomment{#3}{#2}{##1}}%
	\expandafter\newcommand\csname Task#1\endcsname[2][mediumpriority]{\expandafter\TaskPerson[backgroundcolor=##1]{#1}{##2}}%
}
\newcommand*{\Task}[2][mediumpriority]{%
\ifsubmission%
	\else%
	\todo[inline,backgroundcolor=#1]{#2}%
	\fi%
}
\newcommand*{\TaskPerson}[3][]{%
\ifsubmission%
	\else%
	\todo[inline,#1]{\textbf{(#2)} #3}%
	\fi%
}
\newcommand*{\TaskDone}[1]{\TaskPerson[backgroundcolor=done]{Done}{#1}}

\newcommand{\email}[1]{\href{mailto:#1}{\texttt{#1}}}
\newcommand{\MinkPVC}[2]{{#1}^{\star}_{#2}}
\newcommand{\kPVC}[2]{{#1}_{#2}}
\newcommand{\dist}{\mathsf{dist}}
\newcommand{\msize}[1]{\vert #1 \vert}
\newcommand{\Ip}{{I^\prime}}
\newcommand{\Ipp}{{I^{\prime\prime}}}
\newcommand{\Jp}{{J^\prime}}
\newcommand{\Imaxs}{{I^{\max}_s}}
\newcommand{\Jpp}{{J^{\prime\prime}}}
\newcommand{\Gp}{{G^\prime}}
\newcommand{\Gpp}{{G^{\prime\prime}}}
\newcommand{\Hp}{{H^\prime}}
\newcommand{\ReconfSeq}[1]{{\langle #1 \rangle}}
\newcommand{\onestep}[1]{{\overset{#1}{\leftrightarrow}}}
\newcommand{\sevstep}[1]{{\overset{#1}{\leftrightsquigarrow}}}
\newcommand{\sfTS}{\mathsf{TS}}
\newcommand{\sfTJ}{\mathsf{TJ}}
\newcommand{\TS}[2]{\sfTS_{#1}(#2)}
\newcommand{\TJ}[2]{\sfTJ_{#1}(#2)}
\newcommand{\KTS}{\mathcal{K}^{\sfTS}}
\newcommand{\KTJ}{\mathcal{K}^{\sfTJ}}
\newcommand{\Int}{\mathsf{Int}}
\newcommand{\CP}[1]{\text{CP}_{#1}}

% ----------------------------------------------------------------------
% Optional CC BY 4.0 license footer (icon + linked text).
%   \ccbylicense   -> license notice in the footer of the first page
%                     only; call it right after \maketitle
%   \ccbylicense*  -> license notice in the footer of every page
% The wording lives in \ccbynotice; \renewcommand it to adjust the
% text, e.g. to add a copyright holder. The icon comes from the
% ccicons package when it is installed; otherwise the notice is
% text only.
% ----------------------------------------------------------------------
\IfFileExists{ccicons.sty}{%
	\usepackage{ccicons}%
	\newcommand{\ccbyicon}{\ccby\enspace}%
}{%
	\newcommand{\ccbyicon}{}%
}
\newcommand{\ccbynotice}{%
	\ccbyicon This work is licensed under a \href{https://creativecommons.org/licenses/by/4.0/}{Creative Commons Attribution 4.0 International License (CC BY 4.0)}.%
}
\makeatletter
% Page style `ccbyfoot': like `plain' (empty header, centered page
% number), with the notice set below the page number so it extends
% into the bottom margin instead of stealing text height.
\def\ps@ccbyfoot{%
	\let\@mkboth\@gobbletwo
	\let\@oddhead\@empty
	\let\@evenhead\@empty
	\def\@oddfoot{\reset@font\vtop{\hsize\textwidth
		\centering\thepage\par\smallskip
		{\scriptsize\sffamily\ccbynotice\par}}}%
	\let\@evenfoot\@oddfoot}
\newcommand{\ccbylicense}{\@ifstar
	{\pagestyle{ccbyfoot}\thispagestyle{ccbyfoot}}%
	{\thispagestyle{ccbyfoot}}}
\makeatother

\newcommand{\templatelistoftodos}{%
	\ifsubmission
	\else
		\listoftodos
	\fi
}

\newcommand{\printtemplatebibliography}{%
	\ifhasbibfile
		\ifusebiblatex
			\printbibliography
		\else
			\bibliography{refs}
		\fi
	\else
		\paragraph{Bibliography note}
		Add a file named \texttt{refs.bib} when you want to enable the bibliography block in this template, regardless of whether you use the default \texttt{biblatex} mode or the optional \texttt{natbib} mode.
	\fi
}

\hypersetup{
	pdftitle={Self-Contained Article Template},
	pdfauthor={Template Maintainer},
	pdfkeywords={LaTeX template, self-contained article, article workflow}
}

% Author info
\title{\textbf{Self-Contained Article Template}}
\author{Template Maintainer \\ \email{template@example.com}}
\date{\today}

\begin{document}
\maketitle
\ccbylicense

\begin{abstract}
	This document explains how to use the self-contained article template. It shows how to choose build modes, call the helper commands, use the bundled theorem environments, and work with the included table, figure, algorithm, and bibliography support.

	\noindent\textbf{Keywords:} self-contained template, usage guide, LaTeX template
\end{abstract}

\templatelistoftodos

\tableofcontents
% \linenumbers

\section{Quick Start}
\label{sec:quickstart}

The template setup is inlined directly after the document class. It configures the page layout, theorem environments, cross-references, figures, tables, algorithms, todo notes, and bibliography support. A typical edit starts by choosing the build mode near the top of this file and then setting the title information:

\begin{verbatim}
\submissiontrue      % clean output
\usebiblatextrue     % Biber/biblatex workflow

\title{My Document}
\author{Author Name}
\date{\today}

\begin{document}
\maketitle
\templatelistoftodos
\tableofcontents
...
\printtemplatebibliography
\end{document}
\end{verbatim}

\cref{sec:options} explains the build-mode toggles, \cref{sec:helpers} shows the helper commands, \cref{sec:theorems} illustrates the theorem and reference setup, and \cref{sec:assets} shows the figure, table, and algorithm support. For broader LaTeX references, see~\cite{lamport1994latex,mittelbach2004latexcompanion}.

\section{Build Mode Toggles}
\label{sec:options}

The template is controlled by two independent booleans near the top of the preamble: one for review visibility and one for bibliography mode.

\begin{table}[ht]
	\centering
	\begin{tabular}{@{}lll@{}}
		\toprule
		\textbf{Setting} & \textbf{Effect} & \textbf{Typical use} \\
		\midrule
		\texttt{\string\submissiontrue} & Hides todo notes and the todo list & Final or shareable PDF \\
		\texttt{\string\submissionfalse} & Shows todo notes and enables the todo list & Internal review build \\
		\texttt{\string\usebiblatextrue} & Uses Biber through the bibliography helper & Default workflow \\
		\texttt{\string\usebiblatexfalse} & Uses BibTeX through the bibliography helper & BibTeX-only submission flow \\
		\bottomrule
	\end{tabular}
	\caption{Core build toggles provided by the template}
	\label{tbl:options}
\end{table}

\begin{table}[ht]
	\centering
	\begin{tabular}{@{}llcc@{}}
		\toprule
		\multirow{2}{*}{Use case} & \multirow{2}{*}{Goal} & \multicolumn{2}{c}{Recommended setting} \\
		\cmidrule(lr){3-4}
		 & & Visibility & Bibliography \\
		\midrule
		\multirow{2}{*}{Working draft} & Keep review notes visible & \texttt{\string\submissionfalse} & \texttt{\string\usebiblatextrue} \\
		 & Match a BibTeX-only workflow & \texttt{\string\submissionfalse} & \texttt{\string\usebiblatexfalse} \\
		\multirow{2}{*}{Final output} & Hide review notes & \texttt{\string\submissiontrue} & \texttt{\string\usebiblatextrue} \\
		 & Hide notes and use BibTeX & \texttt{\string\submissiontrue} & \texttt{\string\usebiblatexfalse} \\
		\bottomrule
	\end{tabular}
	\caption{Example build-mode combinations}
	\label{tbl:option-combos}
\end{table}

In practice, most final builds use:

\begin{verbatim}
\submissiontrue
\usebiblatextrue
\end{verbatim}

This guide is currently compiled with \verb|\submissionfalse| so that the example notes in \cref{sec:todo-helpers} and the todo list after the title stay visible.
Switch back to \verb|\submissiontrue| when you want a clean PDF.\Task[lowpriority]{Decide the build mode before sharing this PDF.}

\section{Helper Commands}
\label{sec:helpers}

\subsection{Document-Level Helpers}
\label{sec:document-helpers}

The template exposes several helpers that matter in most documents:

\begin{verbatim}
\email{person@example.com}
\templatelistoftodos
\printtemplatebibliography
\ccbylicense
\ccbylicense*
\end{verbatim}

\begin{itemize}
\item \verb|\email{...}| creates a clickable mailto link and is useful in the author block.
\item \verb|\templatelistoftodos| prints the todo list only in \texttt{draft} mode.
\item \verb|\printtemplatebibliography| chooses the bibliography command automatically.
It works with both \texttt{biblatex} and \texttt{natbib}.
\item \verb|\ccbylicense| is optional and prints a CC BY 4.0 notice (icon plus linked text) in the first-page footer, as on the title page of this guide; call it right after \verb|\maketitle|.
The starred form \verb|\ccbylicense*| repeats the notice in the footer of every page.
Renew \verb|\ccbynotice| to adjust the wording, for example to name a copyright holder.
\end{itemize}

\subsection{Todo Commands}
\label{sec:todo-helpers}

Todo helpers are available in every document, but they only render visibly when \verb|\submissionfalse| is set:

\begin{verbatim}
\Task{Rewrite the introduction}
\TaskPerson{Editor}{Check the notation section}
\TaskDone{Bibliography verified}
\TaskNewPerson{Editor}{Editor}{blue}
\Editor{Rewrite the abstract}
\TaskEditor[highpriority]{Update Figure 2}
\end{verbatim}

% Live versions of the commands listed above; visible because this
% guide is compiled with \submissionfalse.
\TaskNewPerson{Editor}{Editor}{blue}
Since this guide is in draft mode, the same commands render right here as live notes\Editor{Rewrite the abstract}:

\Task{Rewrite the introduction}
\TaskPerson{Editor}{Check the notation section}
\TaskDone{Bibliography verified}
\TaskEditor[highpriority]{Update Figure 2}

This design lets one source file serve both as a collaborative draft and as a clean submission build. In \texttt{submission} mode, these commands are effectively silent and the notes above disappear.

\subsection{Domain-Specific Math Helpers}
\label{sec:math-helpers}

The template also bundles a set of graph-reconfiguration macros from the original project. If your article is in that area, they are ready to use:

\[
\TS{k}{G},\quad
\TJ{k}{G},\quad
\ReconfSeq{I_0,\dots,I_t},\quad
I \onestep{\sfTS} J,\quad
\dist(u,v),\quad
\CP{n}.
\]

If your document is unrelated to this notation, you can ignore those commands without affecting the rest of the template.

\section{Theorems and Cross-References}
\label{sec:theorems}

The template predefines common theorem-style environments such as \texttt{theorem}, \texttt{lemma}, \texttt{corollary}, \texttt{proposition}, \texttt{definition}, \texttt{example}, \texttt{remark}, and \texttt{problem}. It also loads \texttt{cleveref}, so references are printed automatically with the correct labels.

\begin{definition}
\label{def:configured-doc}
A \emph{configured document} is a \LaTeX{} file that uses the inlined setup block and its helper commands instead of duplicating preamble setup elsewhere.
\end{definition}

\begin{theorem}
\label{thm:submission-hides-notes}
If a configured document is compiled with \verb|\submissiontrue|, then review annotations created by the todo helpers do not appear in the output.
\end{theorem}

\begin{proof}
The template wraps the todo commands and the todo list helper in conditional checks on the \texttt{submission} flag. When that flag is true, the visible note-producing branches are skipped.
\end{proof}

\begin{example}
\label{ex:cref}
The sentence ``\cref{def:configured-doc,thm:submission-hides-notes} describe the document setup and its clean-output behavior'' is generated with ordinary \verb|\cref| commands; the template handles the labels automatically.
\end{example}

\begin{remark}
If you need a new theorem-like environment that is not already bundled, add it in the setup block so the document receives the same numbering and reference behavior.
\end{remark}

\section{Figures, Tables, and Algorithms}
\label{sec:assets}

The template setup already loads \texttt{graphicx}, \texttt{booktabs}, \texttt{multirow}, \texttt{tikz}, \texttt{adjustbox}, and \texttt{algorithm2e}. Keep figure assets in \texttt{figs/}; the setup sets \verb|\graphicspath{{figs/}}| for you.

\subsection{External Images}
\label{sec:includegraphics}

Typical image commands are available immediately:

\begin{verbatim}
\includegraphics[width=0.45\textwidth]{example}
\includegraphics[height=3cm,keepaspectratio]{example}
\includegraphics[width=0.45\textwidth,trim=1cm 1cm 1cm 1cm,clip]{example}
\end{verbatim}

\begin{figure}[ht]
	\centering
	\begin{minipage}[t]{0.47\textwidth}
		\centering
		\textbf{Width-based scaling}\\[0.5ex]
		\includegraphics[width=\linewidth]{example}
	\end{minipage}\hfill
	\begin{minipage}[t]{0.47\textwidth}
		\centering
		\textbf{Trimmed and clipped}\\[0.5ex]
		\includegraphics[width=\linewidth,trim=1cm 1cm 1cm 1cm,clip]{example}
	\end{minipage}
	\caption{Two common \texttt{\string\includegraphics} patterns using \texttt{figs/example.pdf}}
	\label{fig:includegraphics-examples}
\end{figure}

\subsection{TikZ and Width Control}
\label{sec:tikz}

The template wraps TikZ figures in \texttt{adjustbox} so they scale down automatically when they exceed the text width.

\begin{figure}[ht]
	\centering
	\begin{adjustbox}{max width=\textwidth}
		\begin{tikzpicture}[thick]
			\node[draw, rounded corners, minimum width=3.2cm, minimum height=1cm] (load) at (0,0) {Configure setup};
			\node[draw, rounded corners, minimum width=3.2cm, minimum height=1cm] (write) at (4.5,0) {Write content};
			\node[draw, rounded corners, minimum width=3.2cm, minimum height=1cm] (compile) at (9,0) {Compile PDF};
			\draw[->] (load) -- (write);
			\draw[->] (write) -- (compile);
			\draw[->, bend right=25] (compile.north) to (write.north);
		\end{tikzpicture}
	\end{adjustbox}
	\caption{A simple template workflow drawn directly in the source}
	\label{fig:workflow}
\end{figure}

\subsection{Algorithm Example}
\label{sec:algorithm}

The algorithm environment is ready for methods, procedures, or reproducibility notes:

\begin{algorithm}[ht]
	\caption{Choosing a build configuration}
	\label{alg:workflow}
	\KwIn{A document state and a bibliography preference}
	\KwOut{Build-mode toggles}
	Choose \verb|\submissionfalse| if review notes should remain visible; otherwise choose \verb|\submissiontrue|\;
	Choose \verb|\usebiblatextrue| for the default Biber workflow; otherwise choose \verb|\usebiblatexfalse|\;
	Compile the document and place the bibliography with \verb|\printtemplatebibliography|\;
\end{algorithm}

\subsection{Jeff Erickson's Pseudocode Style}
\label{sec:jeffe-pseudocode}

The template also bundles the boxed pseudocode environments from Jeff Erickson's \texttt{jeffe.sty}, taken from his website (\url{https://jeffe.cs.illinois.edu/}); it is the pseudocode style of his textbook \emph{Algorithms}.
Pseudocode is written line by line inside a \texttt{tabbing}-based box: \verb|\\| starts a new line, \verb|\+| and \verb|\-| change the indentation level, \verb|\mathsc| sets algorithm names in small caps, \verb|\textul| underlines the header line, and \verb|\Comment| produces angle-bracketed comments.

\begin{verbatim}
\begin{algo} ... \end{algo}        % centered, small, boxed pseudocode
\begin{algobox} ... \end{algobox}  % bare boxed block (jeffe's `algorithm')
\begin{code} ... \end{code}        % typewriter block, no box
$\mathsc{MergeSort}(A[1..n])$      % algorithm name in small caps
\Comment{a pseudocode comment}
\end{verbatim}

\begin{algo}
	\textul{$\mathsc{Hanoi}(n, \mathit{src}, \mathit{dst}, \mathit{tmp})$:}\+
	\\ \Comment{Move a stack of $n$ disks from $\mathit{src}$ to $\mathit{dst}$.}
	\\ if $n > 0$\+
	\\ $\mathsc{Hanoi}(n-1, \mathit{src}, \mathit{tmp}, \mathit{dst})$
	\\ move disk $n$ from $\mathit{src}$ to $\mathit{dst}$
	\\ $\mathsc{Hanoi}(n-1, \mathit{tmp}, \mathit{dst}, \mathit{src})$
\end{algo}

The \texttt{algo} environment is not a float, so it has no caption or number; use the \texttt{algorithm2e} environment shown in \cref{sec:algorithm} when you need numbered, captioned algorithm floats.

\section{Bibliography Workflow}
\label{sec:bibliography}

Store bibliography data in \texttt{refs.bib}. The template defaults to \texttt{biblatex} with Biber, but the same source file can switch to \texttt{natbib} with BibTeX by changing the bibliography toggle:

\begin{verbatim}
\usebiblatextrue
\usebiblatexfalse
\end{verbatim}

Use ordinary citation commands in the body, for example
\cite{lamport1994latex,mittelbach2004latexcompanion}.
Finish the document with \verb|\printtemplatebibliography| so the template can
select the correct bibliography command automatically.

\section{Usage Checklist}
\label{sec:checklist}

When you start a new document from this template, the practical checklist is short:
\begin{enumerate}
\item Choose the right visibility and bibliography toggles near the top of the preamble.
\item Keep figure assets in \texttt{figs/} and bibliography data in \texttt{refs.bib}.
\item Place \verb|\templatelistoftodos| near the front matter if you use \texttt{draft} mode.
\item Use the bundled theorem, figure, table, and algorithm environments instead of rebuilding the preamble.
\item End the document with \verb|\printtemplatebibliography|.
\end{enumerate}

\printtemplatebibliography

\end{document}
